# OpenClaw Agent Pool Manager Configuration
# This file is sourced by pool-manager.sh and pool-handler.sh.
# After changing values, restart the service: sudo systemctl restart openclaw-pool

# Maximum number of agent containers running simultaneously.
# Each agent uses ~330MB RAM. Budget: (total_ram - 2GB overhead) / 330MB
MAX_AGENTS=3

# HTTP port for the pool manager API.
POOL_PORT=19000

# Grace period (seconds) — recently active agents are protected from eviction.
EVICT_GRACE_SECONDS=60

# Sync interval (seconds) — how often to reconcile state with Docker.
SYNC_INTERVAL=15

# Heartbeat interval (seconds) — how often containers ping the pool manager.
HEARTBEAT_INTERVAL=30

# Docker Compose project directory.
COMPOSE_DIR=/home/tho/openclaw

# Pool data directory (state, logs, activity files).
POOL_DIR=/home/tho/openclaw/pool
